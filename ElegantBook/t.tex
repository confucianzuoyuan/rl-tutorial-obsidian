\documentclass[lang=cn,newtx,10pt,scheme=chinese]{elegantbook}
\usepackage[dvipsnames]{xcolor}
\usepackage{circuitikz}
\usepackage{annotate-equations}
\usepackage{minted}
\usepackage{algorithm,algpseudocode}
\usetikzlibrary{tikzmark}
\usetikzlibrary{arrows.meta,
                bending,
                intersections,
                quotes,
                shapes.geometric}
\usepackage[demo]{tikzpeople}
\tcbuselibrary{minted,skins,breakable}
% 灵活版本（可选颜色）
\NewDocumentCommand{\mybox}{O{red} m m}{%
  \begin{tcolorbox}[colback=#1!5!white,colframe=#1!75!black,title=#2]
    #3
  \end{tcolorbox}%
}

% 使用 tcolorbox 内置计数器
\newtcblisting[auto counter, number within=section]{mintedbox}[3][]{
    listing engine=minted,
    minted language=#2,
    minted options={
        linenos,
        numbersep=3mm,
        fontsize=\small,
        breaklines,
        autogobble,
    },
    listing only,
    colback=gray!5,
    colframe=blue!75!black,
    title={代码清单 \thetcbcounter: #3},
    label={#1},
    fonttitle=\bfseries,
    top=2mm,
    bottom=2mm,
    left=5mm,
    enhanced,
    overlay={
        \begin{tcbclipinterior}
        \fill[gray!25] (frame.south west) rectangle ([xshift=4mm]frame.north west);
        \end{tcbclipinterior}
    }
}

\begin{document}

\section{第一部分}

代码清单~\ref{code:python1} 展示了 Python 示例。

\begin{mintedbox}[code:python1]{python}{快速排序}
def quicksort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quicksort(left) + middle + quicksort(right)
\end{mintedbox}

\begin{mintedbox}[code:cpp1]{cpp}{二分查找}
int binarySearch(int arr[], int n, int target) {
    int left = 0, right = n - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (arr[mid] == target) return mid;
        else if (arr[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1;
}
\end{mintedbox}

\section{第二部分}

代码清单~\ref{code:java2} 展示了 Java 示例。

\begin{mintedbox}[code:java2]{java}{单例模式}
public class Singleton {
    private static volatile Singleton instance;
    
    private Singleton() {}
    
    public static Singleton getInstance() {
        if (instance == null) {
            synchronized (Singleton.class) {
                if (instance == null) {
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
\end{mintedbox}

\end{document}
