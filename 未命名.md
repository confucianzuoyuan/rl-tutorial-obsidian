在使用策略梯度算法训练智能体时，一个挑战是它们容易出现性能崩溃：智能体会突然表现很差。这种情况很难恢复，因为智能体会开始生成质量较差的轨迹，而这些轨迹又被用于进一步训练策略。我们还看到，on-policy（同策略）算法的样本效率较低，因为它们无法重用数据。

Schulman 等人提出的近端策略优化（Proximal Policy Optimization, PPO）是一类优化算法，用来解决这两个问题。PPO 的核心思想是在目标函数中引入一个代理（surrogate）目标，它通过保证策略的单调改进来避免性能崩溃。该目标还有一个好处，就是可以在训练过程中重用离策略（off-policy）数据。

PPO 可以通过将原始的目标函数 $J(\pi_{\theta})$ 替换为修改后的 PPO 目标，来扩展 REINFORCE 或者 Actor-Critic。此修改带来了更稳定、且样本效率更高的训练过程。

在本章中，我们首先在第 7.1.1 节讨论性能崩溃问题。随后在第 7.1.2 节中，通过单调改进理论来应对该问题。我们将该理论应用于策略梯度目标，将其改造为一个代理目标。

在介绍完理论基础后，我们在第 7.2 节讨论 PPO 算法。接着在第 7.4 节展示如何在 SLM Lab 中将 PPO 作为 Actor-Critic 的扩展进行实现。

## 替代目标

在本节中，我们将介绍用于 PPO 算法的代理（surrogate）目标。首先，我们通过讨论性能崩溃问题来引出这一动机。随后我们将看到如何修改原始的策略梯度目标以避免该问题。

### 性能崩溃

在我们对策略梯度算法的讨论中，我们看到，通过使用策略梯度 $\nabla_{\theta}J(\pi_{\theta})$ 来改变策略参数 $\theta$，即可优化策略 $\pi_{\theta}$。这是一种间接的方法，因为我们是在一个无法直接控制的策略空间中搜索最优策略。要理解原因，我们首先需要区分策略空间与参数空间。

在优化过程中，我们在所有策略的集合中搜索一系列策略 $\pi_{1}, \pi_{2}, \pi_{3},\dots,\pi_{n}$。这个集合被称为策略空间 $\Pi$。

$$
\Pi = \left\{ \pi_{i} \right\} 
$$

当策略被参数化为 $\pi_{\theta}$ 时，我们也可以方便地表示参数 $\theta$ 的参数空间。每一个唯一的 $\theta$ 都对应着一个策略实例。参数空间记为 $\Theta$。

$$
\Theta=\left\{ \theta\in \mathbb{R}^m, \text{其中}\; m \;\text{是策略模型的参数数量} \right\} 
$$

尽管目标函数 $J(\pi_{\theta})$ 是用策略空间中的策略 $\pi_{\theta}\in\Pi$ 所生成的轨迹来计算的，但对最优策略的搜索实际上是在参数空间中通过寻找合适的参数 $\theta\in\Theta$ 来进行的。也就是说，我们的可控量在 $\Theta$，而结果体现在 $\Pi$。在实践中，我们用学习率 $\alpha$ 来控制参数更新的步长，如等式 7.3 所示。

$$
\Delta\theta=\alpha \nabla _{\theta}J(\pi_{\theta})
$$

不幸的是，策略空间与参数空间并不总是同构映射，两者的距离也不一定对应。以两对参数 $(\theta_{1},\theta_{2})$ 和 $(\theta_{2},\theta_{3})$ 为例，假设它们在参数空间中的距离相同，即 $d_{\theta}(\theta_{1},\theta_{2})=d_{\theta}(\theta_{2},\theta_{3})$。然而，它们映射得到的策略对 $(\pi_{\theta_{1}},\pi_{\theta_{2}})$ 与 $(\pi_{\theta_{2}}, \pi_{\theta_{3}})$ 的距离未必相同。也就是说，

$$
d_{\theta}(\theta_{1},\theta_{2})=d_{\theta}(\theta_{2},\theta_{3})\nLeftrightarrow d_{\pi}(\pi_{\theta_{1}},\pi_{\theta_{2}})=d_{\pi}(\pi_{\theta_{2}},\pi_{\theta_{3}})
$$

这带来了潜在问题：参数更新的理想步长 $\alpha$ 难以确定。事先并不清楚它在策略空间 $\Pi$ 中会对应多小或多大的步幅。若 $\alpha$ 过小，就需要很多次迭代，训练会很慢，且策略可能陷入某个局部最优。若 $\alpha$ 过大，策略空间中的对应步长可能越过优良策略的邻域，导致性能崩溃。一旦发生这种情况，新策略会更差，用它生成的轨迹数据也会很差，从而用于下一次更新，进而破坏后续的策略迭代。因此，性能崩溃往往很难恢复。

一般而言，恒定步长并不能避免这些问题。在梯度上升 $\theta\leftarrow\theta+\alpha \nabla_{\theta} J(\pi_{\theta})$ 中，学习率 $\alpha$ 通常是固定的或按计划衰减的，只有方向 $\nabla_{\theta}J(\pi_{\theta})$ 在变化。然而，由于映射是非等距的，恒定步长在映射到 $\Pi$ 时仍可能对应为不同的距离。

最优的 $\alpha$ 选择会随当前策略 $\pi_{\theta}$ 在策略空间 $\Pi$ 中的位置，以及当前参数 $\theta$ 从其在 $\Theta$ 的邻域映射到 $\Pi$ 中 $\pi_{\theta}$ 邻域的方式而变化。理想情况下，算法应能根据这些因素自适应地调整参数步长。

为了基于参数空间中的一次具体更新将如何影响策略空间来推导自适应步长，我们首先需要一种度量，用于衡量两个策略之间性能差异。

### 修改目标

In this section, we introduce the relative policy performance identity which measures the difference in performance between two policies. We then show how this can be used to modify the policy gradient objective so as to guarantee monotonic performance improvement during optimization.

Intuitively, since the problem is with the step size, we can try to introduce a constraint that bounds it to a safe size to prevent performance collapse. By imposing the right constraint, we can derive what is known as the Monotonic Improvement Theory.

First, let a policy $\pi$ be given, and let its next iteration (after a parameter update) be $\pi'$. We can define a relative policy performance identity as the difference in the objectives between them, shown in Equation 7.5. Note that the advantage $A^\pi(s_{t},a_{t})$ is always computed from the older policy $\pi$.

$$
J(\pi')-J(\pi)=\mathbb{E}_{\tau\sim \pi'}\left[ \sum_{t=0}^T\gamma^tA^\pi(s_{t},a_{t}) \right] 
$$

We start by taking the gradient of $J_{\pi}^{\text{CPI}}(\pi')$. To do so, write the variable $\theta$ explicitly. In particular, write the new policy $\pi'$ as a variable $\pi_{\theta}$, and the old policy $\pi$ as a constant $\pi_{\theta_{\text{old}}}$ at which we evaluate the gradient notated $|_{\theta_{\text{old}}}$ (this is necessary because of the explicit dependence of $J_{\pi}^{\text{CPI}}(\pi')$ on $\pi$, which cannot be dropped).

$\pi_{\theta_{\text{old}}}(a_t|s_t) = \pi_\theta(a_t|s_t)|_{\theta_{\text{old}}}$

$$
\max_{\pi_{\text{new}}}J(\pi_{\text{new}}) \Longleftrightarrow \max_{\pi_{\text{new}}}(J(\pi_{\text{new}})-J(\pi_{\text{old}}))
$$









$$
\mathbb{E}[G_{t+1}|S_{t+1}=s_{1}]
$$

$$
\mathbb{E}[G_{t+1}|S_{t+1}=s_{2}]
$$




在强化学习中，带折扣的状态访问分布（discounted state visitation distribution）是一个重要概念。让我详细推导：

## 1. 基本定义

**带折扣的状态访问分布** $d^\pi(s)$ 定义为：

$$d^\pi(s) = (1-\gamma) \sum_{t=0}^{\infty} \gamma^t P(s_t = s | \pi)$$

其中：
- $\gamma \in [0,1)$ 是折扣因子
- $P(s_t = s | \pi)$ 是在策略 $\pi$ 下，时刻 $t$ 访问状态 $s$ 的概率
- $(1-\gamma)$ 是归一化因子

## 2. 详细推导

### 步骤 1：时刻 $t$ 的状态分布

设初始状态分布为 $\mu(s_0)$，则：

$$P(s_t = s | \pi) = \sum_{s_0} \mu(s_0) P(s_0 \to s, t \text{ steps} | \pi)$$

其中 $P(s_0 \to s, t \text{ steps} | \pi)$ 表示从 $s_0$ 出发，经过 $t$ 步到达 $s$ 的概率。

### 步骤 2：递归关系

利用马尔可夫性质：

$$P(s_{t+1} = s' | \pi) = \sum_{s} P(s_t = s | \pi) \sum_{a} \pi(a|s) P(s'|s,a)$$

定义状态转移算子 $\mathcal{P}^\pi$：

$$[\mathcal{P}^\pi d](s') = \sum_{s} d(s) \sum_{a} \pi(a|s) P(s'|s,a)$$

则：$P(s_{t+1} = \cdot | \pi) = \mathcal{P}^\pi P(s_t = \cdot | \pi)$

### 步骤 3：带折扣的累积分布

$$\begin{aligned}
d^\pi(s) &= (1-\gamma) \sum_{t=0}^{\infty} \gamma^t P(s_t = s | \pi) \\
&= (1-\gamma) \left[ \mu(s) + \sum_{t=1}^{\infty} \gamma^t P(s_t = s | \pi) \right] \\
&= (1-\gamma) \mu(s) + (1-\gamma) \sum_{t=1}^{\infty} \gamma^t [\mathcal{P}^\pi]^t \mu(s)
\end{aligned}$$

### 步骤 4：闭式解

利用几何级数求和：

$$d^\pi = (1-\gamma) \sum_{t=0}^{\infty} \gamma^t [\mathcal{P}^\pi]^t \mu$$

当 $\gamma < 1$ 时，这个级数收敛到：

$$d^\pi = (1-\gamma) [I - \gamma \mathcal{P}^\pi]^{-1} \mu$$

其中 $I$ 是单位矩阵。

## 3. 另一种推导方式（Bellman 方程）

$d^\pi$ 满足以下 Bellman 方程：

$$d^\pi(s) = (1-\gamma)\mu(s) + \gamma \sum_{s'} d^\pi(s') \sum_{a} \pi(a|s') P(s|s',a)$$

**证明：**

$$\begin{aligned}
d^\pi(s) &= (1-\gamma) \sum_{t=0}^{\infty} \gamma^t P(s_t = s | \pi) \\
&= (1-\gamma) \mu(s) + (1-\gamma) \sum_{t=1}^{\infty} \gamma^t P(s_t = s | \pi) \\
&= (1-\gamma) \mu(s) + \gamma (1-\gamma) \sum_{t=0}^{\infty} \gamma^t P(s_{t+1} = s | \pi) \\
&= (1-\gamma) \mu(s) + \gamma (1-\gamma) \sum_{t=0}^{\infty} \gamma^t \sum_{s'} P(s_t = s' | \pi) \sum_{a} \pi(a|s') P(s|s',a) \\
&= (1-\gamma) \mu(s) + \gamma \sum_{s'} d^\pi(s') \sum_{a} \pi(a|s') P(s|s',a)
\end{aligned}$$

## 4. 归一化性质

验证 $d^\pi$ 是概率分布：

$$\begin{aligned}
\sum_s d^\pi(s) &= (1-\gamma) \sum_s \sum_{t=0}^{\infty} \gamma^t P(s_t = s | \pi) \\
&= (1-\gamma) \sum_{t=0}^{\infty} \gamma^t \sum_s P(s_t = s | \pi) \\
&= (1-\gamma) \sum_{t=0}^{\infty} \gamma^t \cdot 1 \\
&= (1-\gamma) \cdot \frac{1}{1-\gamma} = 1
\end{aligned}$$

## 5. 直观理解

- **$(1-\gamma)$** 是归一化因子，确保 $d^\pi$ 是概率分布
- **$\gamma^t$** 给予早期访问的状态更高的权重
- 当 $\gamma \to 1$ 时，$d^\pi$ 接近平稳分布（如果存在）
- 当 $\gamma \to 0$ 时，$d^\pi \to \mu$（只关注初始状态）

## 6. 与策略梯度的关系

在策略梯度中，目标函数可以写成：

$$J(\theta) = \mathbb{E}_{s \sim d^{\pi_\theta}, a \sim \pi_\theta} [r(s,a)]$$

或等价地：

$$J(\theta) = \sum_s d^{\pi_\theta}(s) \sum_a \pi_\theta(a|s) r(s,a)$$

这就是为什么 $d^\pi$ 在策略优化中如此重要！





好问题！让我从策略梯度的目标函数出发，详细推导出带折扣的状态访问分布。

## 1. 策略梯度的目标函数

策略梯度法的目标是最大化期望累积回报：

$$J(\theta) = \mathbb{E}_{\tau \sim \pi_\theta} \left[ \sum_{t=0}^{\infty} \gamma^t r(s_t, a_t) \right]$$

其中 $\tau = (s_0, a_0, s_1, a_1, \ldots)$ 是轨迹。

## 2. 展开期望

将期望展开为对所有可能轨迹的求和：

$$J(\theta) = \sum_{\tau} P(\tau | \pi_\theta) \sum_{t=0}^{\infty} \gamma^t r(s_t, a_t)$$

其中轨迹概率为：

$$P(\tau | \pi_\theta) = \mu(s_0) \prod_{t=0}^{\infty} \pi_\theta(a_t|s_t) P(s_{t+1}|s_t, a_t)$$

## 3. 交换求和顺序

关键步骤：交换对轨迹和时间步的求和顺序

$$\begin{aligned}
J(\theta) &= \sum_{\tau} P(\tau | \pi_\theta) \sum_{t=0}^{\infty} \gamma^t r(s_t, a_t) \\
&= \sum_{t=0}^{\infty} \gamma^t \sum_{\tau} P(\tau | \pi_\theta) r(s_t, a_t) \\
&= \sum_{t=0}^{\infty} \gamma^t \mathbb{E}_{\tau \sim \pi_\theta} [r(s_t, a_t)]
\end{aligned}$$

## 4. 边缘化为状态-动作对

对于固定的时刻 $t$，我们可以边缘化出 $(s_t, a_t)$：

$$\begin{aligned}
\mathbb{E}_{\tau \sim \pi_\theta} [r(s_t, a_t)] &= \sum_{s,a} P(s_t=s, a_t=a | \pi_\theta) r(s,a) \\
&= \sum_{s,a} P(s_t=s | \pi_\theta) \pi_\theta(a|s) r(s,a)
\end{aligned}$$

因此：

$$J(\theta) = \sum_{t=0}^{\infty} \gamma^t \sum_{s,a} P(s_t=s | \pi_\theta) \pi_\theta(a|s) r(s,a)$$

## 5. 再次交换求和顺序

现在交换对 $t$ 和 $(s,a)$ 的求和：

$$\begin{aligned}
J(\theta) &= \sum_{s,a} \left[ \sum_{t=0}^{\infty} \gamma^t P(s_t=s | \pi_\theta) \right] \pi_\theta(a|s) r(s,a) \\
&= \sum_{s,a} \left[ \sum_{t=0}^{\infty} \gamma^t P(s_t=s | \pi_\theta) \right] \pi_\theta(a|s) r(s,a)
\end{aligned}$$

## 6. 引入归一化因子

注意到括号内的求和：

$$\sum_{t=0}^{\infty} \gamma^t P(s_t=s | \pi_\theta)$$

这是一个**未归一化**的状态访问频率。为了使其成为概率分布，我们引入归一化因子 $(1-\gamma)$：

$$d^{\pi_\theta}(s) = (1-\gamma) \sum_{t=0}^{\infty} \gamma^t P(s_t=s | \pi_\theta)$$

## 7. 最终形式

因此目标函数可以写成：

$$\boxed{J(\theta) = \frac{1}{1-\gamma} \sum_s d^{\pi_\theta}(s) \sum_a \pi_\theta(a|s) r(s,a)}$$

或者，如果我们重新定义目标函数（常见做法）：

$$\boxed{J(\theta) = \sum_s d^{\pi_\theta}(s) \sum_a \pi_\theta(a|s) r(s,a) = \mathbb{E}_{s \sim d^{\pi_\theta}, a \sim \pi_\theta}[r(s,a)]}$$

这里我们吸收了 $\frac{1}{1-\gamma}$ 常数因子（因为它不影响优化）。

## 8. 完整推导总结

```
原始目标函数：
J(θ) = 𝔼[∑_{t=0}^∞ γᵗ r(sₜ,aₜ)]

↓ 展开期望

J(θ) = ∑_τ P(τ|π) ∑_{t=0}^∞ γᵗ r(sₜ,aₜ)

↓ 交换求和顺序

J(θ) = ∑_{t=0}^∞ γᵗ ∑_{s,a} P(sₜ=s|π) π(a|s) r(s,a)

↓ 再次交换求和

J(θ) = ∑_{s,a} [∑_{t=0}^∞ γᵗ P(sₜ=s|π)] π(a|s) r(s,a)

↓ 定义 d^π(s) = (1-γ) ∑_{t=0}^∞ γᵗ P(sₜ=s|π)

J(θ) = 1/(1-γ) ∑_s d^π(s) ∑_a π(a|s) r(s,a)
```

## 9. 为什么需要 $(1-\gamma)$ 因子？

验证 $d^\pi$ 是概率分布：

$$\begin{aligned}
\sum_s d^\pi(s) &= (1-\gamma) \sum_s \sum_{t=0}^{\infty} \gamma^t P(s_t=s|\pi) \\
&= (1-\gamma) \sum_{t=0}^{\infty} \gamma^t \underbrace{\sum_s P(s_t=s|\pi)}_{=1} \\
&= (1-\gamma) \sum_{t=0}^{\infty} \gamma^t \\
&= (1-\gamma) \cdot \frac{1}{1-\gamma} = 1 \quad \checkmark
\end{aligned}$$

## 10. 策略梯度定理

基于这个推导，策略梯度定理可以表述为：

$$\begin{aligned}
\nabla_\theta J(\theta) &= \nabla_\theta \sum_s d^{\pi_\theta}(s) \sum_a \pi_\theta(a|s) Q^{\pi_\theta}(s,a) \\
&\propto \mathbb{E}_{s \sim d^{\pi_\theta}, a \sim \pi_\theta} [\nabla_\theta \log \pi_\theta(a|s) Q^{\pi_\theta}(s,a)]
\end{aligned}$$

这就是 REINFORCE 算法的理论基础！

## 11. 直观理解

- **$d^\pi(s)$** 告诉我们：在策略 $\pi$ 下，考虑折扣因子后，我们"有多常"访问状态 $s$
- 早期访问的状态（小的 $t$）权重更高（因为 $\gamma^t$ 衰减）
- 这个分布自然地从"最大化折扣累积回报"的目标中产生

这个推导展示了**带折扣的状态访问分布是策略梯度目标函数的自然产物**，而不是人为引入的概念！






$$
\begin{aligned}
\text{Pr}(\tau)&=p(S_{0},A_{0},S_{1},A_{1},\dots,S_{T-1},A_{T-1},S_{T}) \\
&= p(S_{T}|S_{0},A_{0},S_{1},A_{1},\dots)p(S_{0},A_{0},S_{1},A_{1},\dots) \\
&= p(S_{T}|S_{T-1},A_{T-1})p(S_{0},A_{0},S_{1},A_{1},\dots)
\end{aligned}
$$






$$
\delta_{1} = R_{t}+V_{\omega}(S_{t+1}) - V_{\omega}(S_{t})
$$

$$
V_{\omega}(S_{{t+1}}) = R_{t+1} + \gamma{V_{\omega}(S_{t+2})}
$$

$$
\delta_{2} = R_{t}+R_{t+1} + \gamma{V_{\omega}(S_{t+2})} - V_{\omega}(S_{t})
$$


$$
\tau=(S_{0},A_{0},S_{1},A_{1},S_{2},A_{2},S_{3})
$$

$$
\begin{aligned}
Pr(\tau) & = p(S_{0},A_{0},S_{1},A_{1},S_{2},A_{2},S_{3}) \\
& = p(S_{3}|S_{0},A_{0},S_{1},A_{1},S_{2},A_{2})p(S_{0},A_{0},S_{1},A_{1},S_{2},A_{2}) \\
& = p(S_{3}|S_{2},A_{2})p(S_{0},A_{0},S_{1},A_{1},S_{2},A_{2}) \\
& = p(S_{3}|S_{2},A_{2})p(A_{2}|S_{0},A_{0},S_{1},A_{1},S_{2})p(S_{0},A_{0},S_{1},A_{1},S_{2}) \\
& = p(S_{3}|S_{2},A_{2})p(A_{2}|S_{2})p(S_{0},A_{0},S_{1},A_{1},S_{2}) \\
& = p(S_{3}|S_{2},A_{2})\pi_{\theta}(A_{2}|S_{2})p(S_{0},A_{0},S_{1},A_{1},S_{2}) \\
& = p(S_{3}|S_{2},A_{2})\pi_{\theta}(A_{2}|S_{2})p(S_{2}|S_{0},A_{0},S_{1},A_{1})p(S_{0},A_{0},S_{1},A_{1}) \\
& = p(S_{3}|S_{2},A_{2})\pi_{\theta}(A_{2}|S_{2})p(S_{2}|S_{1},A_{1})p(S_{0},A_{0},S_{1},A_{1}) \\
& = p(S_{3}|S_{2},A_{2})\pi_{\theta}(A_{2}|S_{2})p(S_{2}|S_{1},A_{1})p(A_{1}|S_{0},A_{0},S_{1})p(S_{0},A_{0},S_{1}) \\
& = p(S_{3}|S_{2},A_{2})\pi_{\theta}(A_{2}|S_{2})p(S_{2}|S_{1},A_{1})p(A_{1}|S_{1})p(S_{0},A_{0},S_{1}) \\
& = p(S_{3}|S_{2},A_{2})\pi_{\theta}(A_{2}|S_{2})p(S_{2}|S_{1},A_{1})\pi_{\theta}(A_{1}|S_{1})p(S_{0},A_{0},S_{1}) \\
& = p(S_{3}|S_{2},A_{2})\pi_{\theta}(A_{2}|S_{2})p(S_{2}|S_{1},A_{1})\pi_{\theta}(A_{1}|S_{1})p(S_{1}|S_{0},A_{0})p(S_{0},A_{0}) \\
& = p(S_{3}|S_{2},A_{2})\pi_{\theta}(A_{2}|S_{2})p(S_{2}|S_{1},A_{1})\pi_{\theta}(A_{1}|S_{1})p(S_{1}|S_{0},A_{0})p(A_{0}|S_{0})p(S_{0}) \\
\end{aligned}
$$




$$
\pi_{\theta}(a_{t}|s_{t})\gamma \mathbb{N} \mathbb{E} \mathbb{R}  \mathbb{R} \mathbb{Z} \sum \prod 
$$







$$
\text{inv\_freq}=\left[ \frac{1}{\theta^{\frac{0}{8}}}, \frac{1}{\theta^{\frac{2}{8}}}, \frac{1}{\theta^{\frac{4}{8}}}, \frac{1}{\theta^{\frac{6}{8}}} \right] 
$$

$$
\text{positions}=\left[ 0, 1 \right] 
$$

$$
\begin{aligned}
\text{angles}&=\begin{bmatrix}
0 \\
1
\end{bmatrix}
\begin{bmatrix}
\frac{1}{\theta^{\frac{0}{8}}} & \frac{1}{\theta^{\frac{2}{8}}} & \frac{1}{\theta^{\frac{4}{8}}} & \frac{1}{\theta^{\frac{6}{8}}}
\end{bmatrix}\\
&= \begin{bmatrix}
0 & 0 & 0 & 0 \\
1 & \theta^{- \frac{1}{4}} & \theta^{- \frac{1}{2}} & \theta^{- \frac{3}{4}}
\end{bmatrix}
\end{aligned}
$$

$$
\text{angles}=\begin{bmatrix}
0 & 0 & 0 & 0 & 0 & 0 & 0 & 0 \\
1 & \theta^{- \frac{1}{4}} & \theta^{- \frac{1}{2}} & \theta^{- \frac{3}{4}} & 1 & \theta^{- \frac{1}{4}} & \theta^{- \frac{1}{2}} & \theta^{- \frac{3}{4}}
\end{bmatrix}
$$